# Проверенное рабочее состояние

Обновлено 23.09.2026 по согласованной финальной интеграции. Основной источник:
`docs/WORK_STATE.md`; проверки — `docs/acceptance.md` и `docs/integration_notes.md`.

- Приложение, README и команды запуска согласованы с корнем репозитория.
- P1 `7c0a191`, P2 `9a554ce`, P3 `7309b63` объединены с сохранением истории; полные SHA — `BUILD_SOURCES.json`.
- Дублирующая реализация удалена после переноса UI/демо/тестов; `ready_project/` содержит только совместимые точки входа интегратора. Последние модули ядра и ranking сохранены.
- В финальной интеграции исправлена кодировка subprocess в новых end-to-end и optional semantic CLI тестах. Общие production-контракты не менялись.
- Целевая ветка — `main`; публикацию проверять через свежий fetch. Работа выполнялась в отдельной `codex/final-integration`, исходная `feature/ranking` не переключалась.
- Данные: официальный CSV, 66 профилей, исходный файл неизменён. JSONL отсутствует; загрузчик поддерживает оба формата.
- Исправлены ранее выявленные неконечные часы, обход проверки карточек и регистр/пробелы в объяснениях и semantic query.
- Baseline: 193 passed, 5 skipped, 48 subtests; пять сохранённых ответов и AppTest прошли. Установка lock в новом macOS/Python 3.12.13 venv и pip check успешны. HTTP 200/ok.
- С реальным локальным кэшем v2: 196 passed, 2 skipped, 63 subtests. Модель и кэш вне Git; доставка на демонстрацию не подтверждена.
- README/демо актуализированы. Человеческая приёмка и доступ жюри остаются непроверенными.

Следующий шаг людей: пройти `docs/demo.md`, оценить объяснения со скрытыми именами,
проверить запуск на машине демонстрации и доступ к итоговой сборке. При выборе
semantic отдельно передать/подготовить кэш согласно `docs/ranking_notes.md`.

Финальная проверка: код `431c163`, включая новую интеграцию P1 `0b10f4e` и
документацию P3 `3dc7988`. Чистый Git-архив: 193 passed, 5 skipped, 48 subtests;
с локальным кэшем: 196 passed, 2 skipped, 63 subtests; локальная модель/CLI:
35 tests, OK. Протокол — `docs/acceptance.md`.

# Проверенное рабочее состояние

Владелец: участник 1 / координатор.

Обновлено после локальной интеграции 23.09.2026. Основное текущее состояние:
`docs/WORK_STATE.md`; подробная передача и команды: `docs/integration_notes.md`.

## Последний аудит

- Когда и кем: 23.09.2026, участник 1.
- Рабочая ветка: `feature/core`, без push; `main` не изменялся.
- Проверенный кодовый commit: `f8b920a`.
- Fetch выполнен успешно перед интеграцией; более поздние изменения коллег неизвестны.
- Снимки ролей: P1 `8dc60a2`, P2 `c02f788`, P3 `f317487`; все входят в историю текущей сборки.
- Инструкции: `AGENTS.md`, роль интегратора, `codex_team_plan/START_HERE.md`, `TEAM_SPEC.md`, `PARTICIPANT_1.md`.
- Датасет: `docs/hackathon dataset anonymized .csv`, 66 профилей; хэши в integration notes.
- Контракт функций не изменён; Pydantic теперь отклоняет бесконечные часы. Исходные значения сохраняются для показа.
- Оставшееся командное время неизвестно, срок не выдумывался.

## Реализация по проверенным фактам

| Часть | Реализовано? | Проверено? | Доказательство: файл, commit, команда |
|---|---|---|---|
| Загрузка и валидация | Да | Да | test_loader, test_core; конечные/null часы |
| Фильтры и календарь | Да | Да | test_core, test_integration |
| Три исхода и причины | Да | Да | Пять реальных UI-сценариев |
| Ranking и устойчивость | Да, baseline | Да | test_ranking, включая новый процесс |
| Доказательные объяснения | Да | Частично | Тесты фактов прошли; ручная приёмка и дефект регистра остаются |
| AI в продукте | Есть semantic-модуль | Не принят | Нет реального кэша/модели в этой копии; 3 optional-теста пропущены |
| UI и маркировки | Да | Да, автоматически | test_ui + AppTest с реальным CSV/backend |
| Демо на реальном CSV | Пять сценариев | Да, автоматически | Полные demo-артефакты P3 ещё не обновлены; JSONL отсутствует |
| README и чистый запуск | README устарел | Установка проверена | Чистая копия f8b920a, новый venv, lock, pytest; README остаётся P3 |
| Коммиты всех троих | Сохранены | Да | Два merge-коммита; ancestry всех трёх исходных tips подтверждено |

## Подтверждённые блокеры

- Нет блокера запуска baseline на проверенной платформе Windows / Python 3.12.
- P2: `language="РУССКИЙ"` допускается ядром, но даёт неверный текст объяснения.
- P3: README и полные сохранённые ответы не актуализированы; ручная приёмка не выполнена.
- Semantic: передача кэша, воспроизводимая подготовка и нормализация ещё не согласованы.

## Проверки последнего объединения

- `python -m pytest -q -ra`: **165 passed, 3 skipped, 45 subtests passed** в рабочей и отдельной чистой копии.
- `python -m pip check`: **No broken requirements found** в обеих копиях.
- `pip freeze` рабочей среды совпал с 52 закреплёнными пакетами lock.
- HTTP Streamlit: `/` и `/_stcore/health` — 200; проверочный сервер остановлен.
- Ручная приёмка: **not run**. Полные semantic-проверки: **not run**, нет артефактов.

## Следующие решения

P2 исправляет нормализацию объяснений, P3 обновляет README/демо; затем команда
проверяет объяснения без имён. Публикация ветки и выбор semantic требуют
отдельного согласованного шага, не подразумеваются локальной интеграцией.

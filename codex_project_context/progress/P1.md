# Журнал участника 1

23.09.2026. Проверенный кодовый commit: `8e5e1df`.

## Выполнено

- Объединены P2 `0dbee09`, `9a554ce` (merge `10a7fc9`) и P3 `7309b63` (merge `3553740`).
- Сохранены исправления ядра `f8b920a`, включая конечность чисел и защиту карточек.
- Опубликованные UI-демо, полный manifest и end-to-end тесты P3 перенесены в корень.
- Удалены только дубли готовой папки; они восстанавливаются из Git. Старые пути app/run_demo делегируют корневой реализации.
- README согласован с одним источником кода и общим lock. Исправлен Unicode-вывод demo CLI на Windows.
- Добавлены регрессии нормализации baseline, совместимого запуска и отсутствия второй реализации.
- Журналы P2/P3 не изменялись.

## Проверки

`python -m pytest -q -ra`: 193 passed, 5 skipped, 48 subtests passed.
`python run_demo.py --verify`: OK: 5 saved responses match the real core.
`python -m pip check`: No broken requirements found.
Ссылки корневого README проверены: отсутствующих локальных файлов нет.
Подробности чистой установки и публикации — в отчёте интеграции.

## Осталось человеку/команде

Человеческая оценка объяснений без имён. Semantic — только после передачи кэша/модели и отдельной проверки целевой среды.

# Журнал участника 2

Единственный редактор: участник 2 / его Codex. Координатор читает опубликованный журнал, но не пишет за владельца.

## Последний подтверждённый снимок

- Роль: 2.
- Ветка: `feature/ranking`.
- Последний проверенный кодовый commit: `c02f788`; нормализация semantic-запроса и регрессии — рабочая копия до отдельного коммита, hash в отчёте.
- Полученный общий baseline/контракт: опубликованный `origin/main` `82a8225`, merge `d5ab8b7`; PROJECT_SPEC сохраняет сигнатуры TEAM_SPEC.
- Свежесть сведений: `git fetch origin` успешен 23.09.2026; main=`82a8225`, UI=`f317487`, впервые опубликовано ядро `feature/core=8dc60a2`. Прочитаны его контракт, loader/recommend, WORK_STATE и интеграционные тесты. P1 разрешил предоставленный CSV; JSONL отсутствует. Нового пакета codex_project_context в core нет; общая доска в main ещё стартовая.
- Текущая задача: A23/A24 — согласовать внутреннюю нормализацию semantic и объяснений с уже опубликованным strip/casefold ядра. Общий контракт не менялся.
- Статус: ready_for_merge; во временном снимке проверен путь CSV → ядро → ranking/cards → UI, включая новый semantic-кэш v2. Объединение веток не выполнено; один устаревший тест P1 требует исправления владельцем.

## Что действительно сделано

- A21/A22: опубликован `f608398`, базовый порядок по цене/ID, evidence, карточки и 30 исходных тестов.
- A23: кэш embeddings с закреплённой моделью/revision, проверкой SHA-256, неизменяемым снимком и явным выбором режима при запуске.
- Semantic score — cosine по всему description; цена и ID разбирают равенства. Никаких новых фильтров или срезов TOP-3 внутри ranking.
- Длинные тексты сохраняются полностью через части в пределах token limit; предложение для цитаты выбирается по cosine, остаётся буквальным и проверяется по координатам.
- Проверена настоящая модель `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, revision `e8f8c211226b894fcb81acc59f3b34ba3efd5f42`; инференс локальный CPU, данные наружу не передаются.
- Общие dependencies/.env/контракт/loader/ядро/UI не менялись. Пакеты/веса/артефакт эксперимента — только во временной папке вне Git. Default остаётся baseline.
- A24: устранён воспроизводимый пробел HK-42352 — разные свободные даты и запрошенные 6/10 ч раньше давали одинаковые тексты. Теперь добавляются факты календаря, запрошенных языка/часов; busy_dates включён в evidence копированием.
- При одинаковых цене и цитате различные max_hours дают проверяемое отличие текста, если длительность запрошена. Missing/None календарь не доказывает свободу; max_hours=null не становится «безлимитом». Score/порядок/семантические цитаты не меняются.
- Выполнена модульная проверка dense/date_b/rare/no_match/missing_category из снимка P3 `5f85877`; queries и ID-списки регрессионных фикстур отдельно сверены с источником. В обоих режимах при смене даты из TOP-3 исчезают HK-88430/HK-44923, занятые 08.10; редкая категория даёт две карточки, пустые входы не дополняются.
- В предыдущем шаге `9857b7d` добавлены только регрессии подготовленных списков; текущая проверка настоящего ядра описана ниже.

## Результат текущего шага и проверки

- Главный риск воспроизведён: ядро допускает КОРПОРАТИВ/ведущий, semantic-кэш раньше отвечал text missing. Новые регрессии падали до правки.
- query_text теперь применяет strip().casefold() к event_type/category при подготовке и lookup. Descriptions и исходные query/profile evidence не меняются; неизвестные тексты не получают fallback.
- В explanations устранено аналогичное несоответствие для формата/языка: РУССКИЙ считается указанным, если профиль содержит русский. Состав карточек, фильтры и публичные сигнатуры сохранены.
- Внутренняя TEXT_VERSION=`event-category-casefold-ru-excerpts320-v2`. Старый v1-кэш явно отклоняется, необходима подготовка embeddings заново.
- Две локальные генерации закреплённой моделью дали одинаковый SHA-256 `49ba8fbda223ab7a0dec5377630eea9376655a18be203611deedd083b33baf60`; новый путь `/private/tmp/hackalem-semantic.p3n3Jp/normalized-v2/embeddings.json`. Старый артефакт сохранён, общий requirements не изменён.
- Пять ожидаемых TOP-3 сохранились. Численные cosine изменились из-за нормализованного входа модели; это новая версия артефакта, не переименование прежнего.
- Свои тесты с ядром/кэшем: 75 passed, 1 optional skipped. Stdlib без артефактов: 71 passed, 5 skipped. Отдельно semantic tests с настоящей моделью: все 29 passed.
- Общий suite P1+P2+P3: 127 passed, 1 failed, 1 skipped; остаётся известный тест отсутствия ranking участника 1. Пять реальных AppTest-сценариев с v2-кэшем прошли без подмены ядра.
- Read-only ревью замечаний к нормализации/evidence не выявило. Точные команды и ограничения — в последнем разделе docs/ranking_notes.md.

## Изменённые файлы и передача

Только semantic.py, explanations.py, три собственных тестовых файла, docs/ranking_notes.md и этот журнал. Ветка feature/ranking; предшествующий фикс экспорта c02f788 опубликован. Hash текущей правки — в отчёте.
Чужие файлы, shared dependencies и контракты не изменены. Ядро/UI проверялись во временном снимке, не объединялись в нашу рабочую ветку.

## Запросы другим участникам

- **P1 / C02 / тест:** `feature/core@8dc60a2`, `tests/test_core.py:193`, `test_success_requires_participant_two_module` предполагает отсутствие ranking без изоляции. После подключения наш публичный вызов работает, поэтому ошибка `DID NOT RAISE IntegrationNotReadyError`. Нужно явно имитировать отсутствие модуля при импорте (ModuleNotFoundError с name=ranking); успешный путь закреплён нашими тестами. Чужой тест не менял.
- **P1 / интеграция:** получить feature/ranking с экспортом build_cards и новой нормализацией; объединить ветки с сохранением авторства, исправить принадлежащий вам тест и закрепить общий запуск с CSV. Во временном снимке настоящий UI уже прошёл пять сценариев.
- **P1 / semantic:** теперь требуется v2-кэш с SHA-256 49ba8fbda223ab7a0dec5377630eea9376655a18be203611deedd083b33baf60; v1 не принимается. Размещение артефакта и окружение подготовки ещё требуют решения интегратора. До импорта задаются RANKING_MODE=semantic, RANKING_SEMANTIC_CACHE, RANKING_SEMANTIC_SHA256; метаданные брать из ranking. Runtime — stdlib; shared requirements самостоятельно не менял. Default — baseline.
- **P1 / C01:** обновить общую доску/состояние по опубликованным коммитам: они пока стартовые, а core-ветка не содержит нового пакета контекста.
- **P3 / A33:** пять запросов теперь подтверждены через настоящий loader/recommend в обоих режимах. Таблица TOP-3 в ranking_notes сохраняется. CSV-путь также проверен через настоящий UI в отдельной сборке; verified_cases и README обновляет владелец после своей проверки.

## Следующая задача и проверка человека

Дальше интегратор объединяет ветки, исправляет тест отсутствия ranking и выбирает способ поставки v2-кэша. P3 закрепляет реальные UI-демо в документации. Содержательность объяснений требует человеческой приёмки A24: скрыть имена у трёх карточек dense и сопоставить тексты с исходными профилями. Отдельных новых алгоритмических изменений без обнаруженного дефекта сейчас не требуется.

## История законченных шагов

- 23.09.2026, A21/A22: 30 тестов, commit `f608398`, опубликован в origin/feature/ranking.
- 23.09.2026, контекст: получен `82a8225`, безопасный merge `d5ab8b7`, прежняя работа сохранена.
- 23.09.2026, A23: 53 обычных проверки + прошедший optional real-model smoke; повтор генерации кэша в новом процессе побайтно совпал. Кэш SHA-256 `bd5b481f3538126ba2daf5de5c255185cdbcf8bd39c9ee0edb91c8ac8c6da09c`; сведения и границы в docs/ranking_notes.md.
- 23.09.2026, A23 опубликован: commit `8f4c2bb` в origin/feature/ranking.
- 23.09.2026, A24: 9 новых регрессий, 63 теста с настоящей моделью passed; только свои четыре файла, отдельный commit после проверки diff. Полное приложение не объявляется проверенным.
- 23.09.2026, A24 опубликован: `2cf4aeb`.
- 23.09.2026, prepared_cases P3 `5f85877`: 5 новых тестов, все 68 passed с локальной моделью/кэшем; порядок и evidence повторяются в новых процессах. Ожидаемые TOP-3 сохранены для интегратора, полное приложение не проверено.

- 23.09.2026, стык C02/A24 с core `8dc60a2`: исправлен публичный экспорт build_cards; 70 собственных проверок с ядром/кэшем passed, 1 optional skipped. В общем suite 95 passed, 1 устаревший тест P1 failed, 1 skipped; адресное сообщение владельцу выше.

- 23.09.2026, A23/A24: strip/casefold нормализация и кэш v2 проверены на настоящем ядре, модели и UI. 5 новых регрессий, TOP-3 пяти демо сохранён; публикация отдельным коммитом после проверки diff.

# Журнал участника 2

Единственный редактор: участник 2 / его Codex. Координатор читает опубликованный журнал, но не пишет за владельца.

## Последний подтверждённый снимок

- Роль: 2.
- Ветка: `feature/ranking`.
- Последний проверенный кодовый commit: `2cf4aeb` (A21–A24); проверка подготовленных сценариев P3 — рабочая копия до отдельного тестового коммита, hash в отчёте.
- Полученный общий baseline/контракт: опубликованный `origin/main` `82a8225`, merge `d5ab8b7`; PROJECT_SPEC сохраняет сигнатуры TEAM_SPEC.
- Свежесть сведений о других ролях: новый `git fetch origin` успешен 23.09.2026; main=`82a8225`, UI=`5f85877`. Прочитаны новые prepared_cases в demo_queries.json и изменения docs/demo.md / docs/acceptance.md. Доска/состояние ещё стартовые, настоящего ядра/JSONL в опубликованном снимке нет.
- Текущая задача: A24 — проверить ranking/cards на пяти подготовленных списках допустимых кандидатов участника 3 в baseline и semantic, передать ожидаемые порядки для C02/A33.
- Статус: ready_for_merge (модуль и проверки); интеграция в приложение не подтверждена.

## Что действительно сделано

- A21/A22: опубликован `f608398`, базовый порядок по цене/ID, evidence, карточки и 30 исходных тестов.
- A23: кэш embeddings с закреплённой моделью/revision, проверкой SHA-256, неизменяемым снимком и явным выбором режима при запуске.
- Semantic score — cosine по всему description; цена и ID разбирают равенства. Никаких новых фильтров или срезов TOP-3 внутри ranking.
- Длинные тексты сохраняются полностью через части в пределах token limit; предложение для цитаты выбирается по cosine, остаётся буквальным и проверяется по координатам.
- Проверена настоящая модель `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, revision `e8f8c211226b894fcb81acc59f3b34ba3efd5f42`; инференс локальный CPU, данные наружу не передаются.
- Общие dependencies/.env/контракт/loader/ядро/UI не менялись. Пакеты/веса/артефакт эксперимента — только во временной папке вне Git. Default остаётся baseline.
- A24: устранён воспроизводимый пробел HK-42352 — разные свободные даты и запрошенные 6/10 ч раньше давали одинаковые тексты. Теперь добавляются факты календаря, запрошенных языка/часов; busy_dates включён в evidence копированием.
- При одинаковых цене и цитате различные max_hours дают проверяемое отличие текста, если длительность запрошена. Missing/None календарь не доказывает свободу; max_hours=null не становится «безлимитом». Score/порядок/семантические цитаты не меняются.
- Выполнена модульная проверка dense/date_b/rare/no_match/missing_category из снимка P3 `5f85877`; queries и ID-списки регрессионных фикстур отдельно сверены с источником. В обоих режимах при смене даты из TOP-3 исчезают HK-88430/HK-44923, занятые 08.10; редкая категория даёт две карточки, пустые входы не дополняются.
- Production-код в этом шаге не изменён; добавлены только собственные регрессии и отчёт. Бизнес-статусы и фильтрация ядра не объявляются проверенными.

## Проверки

| Команда/проверка | Фактический результат | Где воспроизвести |
|---|---|---|
| `RANKING_MODE=baseline python3 -B -m unittest discover -s tests -p 'test_*.py' -q` | 66 тестов passed; два optional теста модели/кэша skipped | Корень feature/ranking, stdlib |
| Та же команда через временный venv с SEMANTIC_TEST_MODEL_DIR, SEMANTIC_DEMO_CACHE и HF_HUB_OFFLINE=1 | passed: все 68 тестов, 5.553 s | Точная команда в docs/ranking_notes.md |
| Snapshot queries/IDs против demo_queries.json P3 `5f85877` | passed: полное совпадение пяти запросов и списков | Источник прочитан через git show, чужие файлы не менялись |
| Два независимых запуска локального probe.py на предоставленном CSV | passed: одинаковые байты кэша, TOP-3 и explanations | /private/tmp/hackalem-semantic.p3n3Jp/report-first.json и report.json |
| 24 перестановки semantic-входа в отдельных процессах | passed | tests/test_ranking.py |
| Независимое read-only ревью A24 | замечаний по дате/языку/часам/evidence и сохранению состава нет; 62 passed + 1 skip независимо повторены | Текущие тесты и итоговый запуск |
| Полный JSONL → recommend → UI | not run: JSONL и ядро отсутствуют в полученном снимке | Владелец интеграции — участник 1 |

## Изменённые файлы и передача

- Собственные изменения этого шага: tests/test_ranking.py, docs/ranking_notes.md, этот progress/P2.md. Чужие файлы и production-код не изменены.
- Коммит/ветка для передачи: `feature/ranking`; A23=`8f4c2bb`, исправления A24=`2cf4aeb`, hash новых demo-регрессий в отчёте после коммита.
- Готово к объединению: регрессии на подготовленных сценариях P3 и таблица ожидаемого TOP-3 обоих режимов. Итоговая человеческая приёмка A24 ждёт интеграции.
- Интеграция в общий проект: ещё не подтверждена; координатор должен принять артефакт/настройки и выполнить сквозную проверку.

## Запросы другим участникам

- Участнику 1 → A23/C02 → для включения semantic нужны утверждённый JSONL и размещение кэша → подготовить cache через prepare_semantic_cache, передать RANKING_MODE=semantic, RANKING_SEMANTIC_CACHE, RANKING_SEMANTIC_SHA256 до импорта ranking. Метаданные брать из ranking.RANKING_METHOD/SCORING_VERSION. Полный протокол/пример в docs/ranking_notes.md.
- Участнику 1 → окружение подготовки → принять пины ST 6.1.0 / transformers 5.17.0 / torch 2.14.0 и полный lock только для подготовки либо принять уже проверенный артефакт. Runtime кэша использует только stdlib. Общие зависимости самостоятельно не менял.
- Участнику 1 → C01 → стартовые общие документы ещё не отражают A21/A22/A23 → обновить доску/WORK_STATE по фактическим коммитам; сигнатуры rank_candidates(candidates,query), build_cards(top_candidates,query) сохранены.
- Участнику 3 → A32/A34 → в README различать baseline и semantic: модель использована при подготовке embeddings, запрос ранжируется локально по кэшу; нет нейросетевой генерации свободного текста и скрытого fallback. Цена «от» и подготовленные значения сохранены.
- Участнику 3 → A24 → explanation теперь явно отражает календарную дату и запрошенные язык/часы; UI может выводить его без преобразований. Нужна человеческая проверка различимости на настоящих финалистах после C02; сравнение с max_hours=8/10 уже закреплено синтетическим тестом.
- Участникам 1/3 → C02/A33 → для prepared_cases `5f85877` передана таблица baseline/semantic в конце docs/ranking_notes.md. Смена даты 06.10 → 08.10 действительно убирает HK-88430/HK-44923 из TOP-3 обоих режимов. Это ranking на переданных reference-списках, не observed_response настоящего recommend; не переносить в verified_cases до сквозного прогона.

## Следующая задача и проверка человека

Следующая общая проверка — C02/A33: соединить с настоящим ядром и JSONL, затем завершить человеческую приёмку A24 по реальному интерфейсу.
Одна человеческая проверка после подключения: запустить dense и date_b участника 3 и убедиться, что исчезнувшие HK-88430/HK-44923 объяснены занятостью, а не просто пропали без причины.
Пробный запрос: Алматы, 2026-09-30, Ведущий, свадьба, 1 000 000 ₸, язык/часы не заданы; TOP-3 HK-42352 → HK-35215 → HK-77838. Это reference-эксперимент, не подтверждённый результат приложения.

## История законченных шагов

- 23.09.2026, A21/A22: 30 тестов, commit `f608398`, опубликован в origin/feature/ranking.
- 23.09.2026, контекст: получен `82a8225`, безопасный merge `d5ab8b7`, прежняя работа сохранена.
- 23.09.2026, A23: 53 обычных проверки + прошедший optional real-model smoke; повтор генерации кэша в новом процессе побайтно совпал. Кэш SHA-256 `bd5b481f3538126ba2daf5de5c255185cdbcf8bd39c9ee0edb91c8ac8c6da09c`; сведения и границы в docs/ranking_notes.md.
- 23.09.2026, A23 опубликован: commit `8f4c2bb` в origin/feature/ranking.
- 23.09.2026, A24: 9 новых регрессий, 63 теста с настоящей моделью passed; только свои четыре файла, отдельный commit после проверки diff. Полное приложение не объявляется проверенным.
- 23.09.2026, A24 опубликован: `2cf4aeb`.
- 23.09.2026, prepared_cases P3 `5f85877`: 5 новых тестов, все 68 passed с локальной моделью/кэшем; порядок и evidence повторяются в новых процессах. Ожидаемые TOP-3 сохранены для интегратора, полное приложение не проверено.

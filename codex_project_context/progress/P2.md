# Журнал участника 2

Единственный редактор: участник 2 / его Codex. Координатор читает опубликованный журнал, но не пишет за владельца.

## Последний подтверждённый снимок

- Роль: 2.
- Ветка: `feature/ranking`.
- Последний проверенный кодовый commit: `f608398` (A21/A22); A23 — рабочая копия до коммита, hash в отчёте этапа.
- Полученный общий baseline/контракт: опубликованный `origin/main` `82a8225`, merge `d5ab8b7`; PROJECT_SPEC сохраняет сигнатуры TEAM_SPEC.
- Свежесть сведений о других ролях: `git fetch origin` успешно 23.09.2026; общая доска/состояние в доступном пакете ещё стартовые, JSONL/ядро в этом снимке отсутствуют.
- Текущая задача: A23 — локальные embeddings, фиксированный кэш/режим, semantic evidence и объяснения.
- Статус: ready_for_merge (модуль и проверки); интеграция в приложение не подтверждена.

## Что действительно сделано

- A21/A22: опубликован `f608398`, базовый порядок по цене/ID, evidence, карточки и 30 исходных тестов.
- A23: кэш embeddings с закреплённой моделью/revision, проверкой SHA-256, неизменяемым снимком и явным выбором режима при запуске.
- Semantic score — cosine по всему description; цена и ID разбирают равенства. Никаких новых фильтров или срезов TOP-3 внутри ranking.
- Длинные тексты сохраняются полностью через части в пределах token limit; предложение для цитаты выбирается по cosine, остаётся буквальным и проверяется по координатам.
- Проверена настоящая модель `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, revision `e8f8c211226b894fcb81acc59f3b34ba3efd5f42`; инференс локальный CPU, данные наружу не передаются.
- Общие dependencies/.env/контракт/loader/ядро/UI не менялись. Пакеты/веса/артефакт эксперимента — только во временной папке вне Git. Default остаётся baseline.

## Проверки

| Команда/проверка | Фактический результат | Где воспроизвести |
|---|---|---|
| `RANKING_MODE=baseline python3 -B -m unittest discover -s tests -p 'test_*.py' -q` | passed: 53 теста; optional local-model smoke skipped | Корень feature/ranking, stdlib |
| Та же команда через временный venv с SEMANTIC_TEST_MODEL_DIR и HF_HUB_OFFLINE=1 | passed: все 54 теста, включая настоящую модель | Точная команда в docs/ranking_notes.md |
| Два независимых запуска локального probe.py на предоставленном CSV | passed: одинаковые байты кэша, TOP-3 и explanations | /private/tmp/hackalem-semantic.p3n3Jp/report-first.json и report.json |
| 24 перестановки semantic-входа в отдельных процессах | passed | tests/test_ranking.py |
| Независимое read-only ревью | замечаний по финальным semantic cache/excerpt/evidence путям не осталось; его 53 теста проверены основным агентом | Текущие тесты и итоговый запуск |
| Полный JSONL → recommend → UI | not run: JSONL и ядро отсутствуют в полученном снимке | Владелец интеграции — участник 1 |

## Изменённые файлы и передача

- Собственные изменения A23: semantic.py, ranking.py, explanations.py, tests/test_semantic.py, tests/test_ranking.py, tests/test_explanations.py, docs/ranking_notes.md, этот progress/P2.md.
- Коммит/ветка для передачи: `feature/ranking`; точный hash A23 в отчёте после коммита.
- Готово к объединению: реализация модуля A23 и его тесты; базовый API совместим.
- Интеграция в общий проект: ещё не подтверждена; координатор должен принять артефакт/настройки и выполнить сквозную проверку.

## Запросы другим участникам

- Участнику 1 → A23/C02 → для включения semantic нужны утверждённый JSONL и размещение кэша → подготовить cache через prepare_semantic_cache, передать RANKING_MODE=semantic, RANKING_SEMANTIC_CACHE, RANKING_SEMANTIC_SHA256 до импорта ranking. Метаданные брать из ranking.RANKING_METHOD/SCORING_VERSION. Полный протокол/пример в docs/ranking_notes.md.
- Участнику 1 → окружение подготовки → принять пины ST 6.1.0 / transformers 5.17.0 / torch 2.14.0 и полный lock только для подготовки либо принять уже проверенный артефакт. Runtime кэша использует только stdlib. Общие зависимости самостоятельно не менял.
- Участнику 1 → C01 → стартовые общие документы ещё не отражают A21/A22/A23 → обновить доску/WORK_STATE по фактическим коммитам; сигнатуры rank_candidates(candidates,query), build_cards(top_candidates,query) сохранены.
- Участнику 3 → A32/A34 → в README различать baseline и semantic: модель использована при подготовке embeddings, запрос ранжируется локально по кэшу; нет нейросетевой генерации свободного текста и скрытого fallback. Цена «от» и подготовленные значения сохранены.

## Следующая задача и проверка человека

Следующая общая проверка — C02/A33: соединить с настоящим ядром и JSONL, затем A24 по реальному интерфейсу.
Одна человеческая проверка сейчас: сопоставить без имён три объяснения пробного CSV-запроса из docs/ranking_notes.md с исходными descriptions; отдельно отметить, если цитата не помогает понять выбор.
Пробный запрос: Алматы, 2026-09-30, Ведущий, свадьба, 1 000 000 ₸, язык/часы не заданы; TOP-3 HK-42352 → HK-35215 → HK-77838. Это reference-эксперимент, не подтверждённый результат приложения.

## История законченных шагов

- 23.09.2026, A21/A22: 30 тестов, commit `f608398`, опубликован в origin/feature/ranking.
- 23.09.2026, контекст: получен `82a8225`, безопасный merge `d5ab8b7`, прежняя работа сохранена.
- 23.09.2026, A23: 53 обычных проверки + прошедший optional real-model smoke; повтор генерации кэша в новом процессе побайтно совпал. Кэш SHA-256 `bd5b481f3538126ba2daf5de5c255185cdbcf8bd39c9ee0edb91c8ac8c6da09c`; сведения и границы в docs/ranking_notes.md.

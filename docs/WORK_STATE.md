# Текущее состояние работы

Обновлять кратко после решений, изменения общих контрактов и завершения значимой части работы. Здесь находится только текущее состояние, а не подробный дневник.

## Сейчас

- Выбранная задача: «Умный подбор подрядчиков» (#79-lite).
- Текущая цель: передать участникам общий контракт и интегрировать `ranking.py`, когда участник 2 его подготовит.
- Рабочий сценарий: запрос → валидация → город/категория → обязательные фильтры → ranking всех допустимых → срез до 3 → карточки → единый ответ.
- Главный блокер: успешная выдача в production ждёт `rank_candidates` и `build_cards` участника 2; пустые бизнес-статусы и всё ядро уже работают независимо.

## Распределение

| Участник или агент | Ожидаемый результат | Принадлежащие файлы | Входы и фиксированные интерфейсы | Проверка | Статус |
|---|---|---|---|---|---|
| Участник 1 | Контракт, загрузка CSV/JSONL, фильтры, статусы, единый ответ и интеграционная обвязка | `contracts.py`, `data_loader.py`, `recommender.py`, `requirements.txt`, `requirements.lock`, `pytest.ini`, `tests/test_core.py`, `tests/test_loader.py`, `tests/test_integration.py`, интеграторские документы | Контракт `TEAM_SPEC`; ranking подключается как `ranking.py` | `python -m pytest -q` | Реализовано, тесты проходят |
| Участник 2 | Устойчивый ranking всех допустимых и карточки финальной тройки | `ranking.py`, собственные тесты | `rank_candidates(candidates, query)`, `build_cards(top_candidates, query)`; исходные профили не мутировать | Контрактные и детерминированные тесты | Ожидается |
| Участник 3 | UI, README, демо и приёмка | UI-файлы, `README.md`, свои проверки | Отображать ответ `recommend` без повторного отбора/сортировки | Живые демо-запросы | Ожидается |

## Общие контракты

Ссылка на актуальные контракты в `docs/CHALLENGE_BRIEF.md` или коде. Изменения общего контракта сначала согласовывает интегратор, затем сообщает затронутым участникам.

- Схема и правила: `codex_team_plan/TEAM_SPEC.md`.
- Исполняемые модели и ошибки: `contracts.py`.
- Участник 2 возвращает весь список допустимых; только `recommend` делает `[:3]` и затем вызывает `build_cards`.
- До появления `ranking.py` успешный запрос явно поднимает `IntegrationNotReadyError`; `category_not_found` и `no_matching_candidates` возвращаются полностью.

## Принятые решения

| Решение | Причина | Последствие |
|---|---|---|
| Pydantic для строгих схем, pytest для тестов | Популярные поддерживаемые MIT-библиотеки снижают риск ручной валидации и проверки | Точные версии закреплены в `requirements.txt` и `requirements.lock` |
| CSV и JSONL через единый загрузчик | Официальный CSV есть сейчас, JSONL назван в задании | Оба формата дают одинаковые нормализованные словари |
| Ranking не дублировать в ядре | Владение участника 2 и общий контракт | Интеграция проверяет состав, порядок карточек и сохранность исходных полей |

## Блокеры и вопросы

| Вопрос | Что уже выяснено | Рекомендуемый вариант | Кто решает |
|---|---|---|---|
| Когда будет готов `ranking.py`? | Ядро ожидает две функции и не подменяет их заглушкой | Передать участнику 2 контракт и интеграционные ошибки | Участник 2 |
| Нужен ли организатору именно JSONL? | Все 66 профилей и поля доступны в официальном CSV; загрузчик уже поддерживает JSONL | Продолжать с CSV, запросить JSONL только если это требование сдачи | Организатор/команда |

## Следующие действия

1. Передать участнику 2 контракт `ranking.py` и дождаться сохранённого коммита.
2. Передать участнику 3 схему response и честное состояние интеграции.
3. После получения ranking заменить тестовый адаптер в сквозной проверке реальным модулем и запустить приложение.

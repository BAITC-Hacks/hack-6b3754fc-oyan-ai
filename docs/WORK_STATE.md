# Текущее состояние работы

Обновлено 23.09.2026 при согласованной пользователем финальной интеграции.

## Сейчас

- Задача: «Умный подбор подрядчиков» (#79-lite).
- Единое приложение находится в корне репозитория. Дублирующая реализация удалена; `ready_project/` содержит совместимые точки входа интегратора, которые запускают корневое приложение.
- Исходники: P1 `7c0a191`, P2 `9a554ce`, P3 `7309b63`; полные SHA — `BUILD_SOURCES.json`. История сохранена merge-коммитами `1a16265` и `285a720` поверх предыдущей интеграции P1.
- Целевая ветка публикации — `main`. Результат публикации проверяется по remote, а не по этому документу.
- Основной режим — baseline. Нормализация объяснений и semantic query v2 исправлена; CLI подготовки кэша включён. Модель и кэш вне Git.
- Новый venv macOS/Python 3.12.13: установка lock и pip check прошли. Общий набор: 193 passed, 5 skipped, 48 subtests; с локальным кэшем: 196 passed, 2 skipped, 63 subtests.
- Все пять сохранённых ответов совпали с реальным ядром, пройдены через AppTest; HTTP `/` и health — 200. Полный протокол: `docs/acceptance.md`.

## Владение и контракты

- P1: `contracts.py`, `data_loader.py`, `recommender.py`, общие зависимости и интеграторские документы.
- P2: `ranking.py`, `scoring.py`, `semantic.py`, `explanations.py`, собственные тесты и `docs/ranking_notes.md`.
- P3: `app.py`, `run_demo.py`, README, демо-артефакты и UI/сквозные тесты.
- Финальный перенос и обновление общих документов выполнены по прямому поручению пользователя; обычные ролевые границы сохраняются. Чужие журналы не переписывались.
- Исполняемые схемы — `contracts.py`, описание — `docs/CHALLENGE_BRIEF.md`. Общие интерфейсы финальным переносом не менялись.
- `rank_candidates` возвращает всех допустимых; только `recommend` делает `[:3]`, затем вызывает `build_cards`. Ядро проверяет состав, порядок и исходные поля.
- Положительные `duration/max_hours` конечны; `None` допустим. Источник UI — официальный CSV относительно `app.py`; `CONTRACTORS_DATA` переопределяет путь.

## Оставшиеся действия

1. Люди P2/P3: пройти `docs/demo.md`, проверить полезность и различимость объяснений со скрытыми именами; записать фактический результат.
2. Команда: проверить доступ жюри к итоговому репозиторию либо передать архив.
3. Только если выбран semantic для демонстрации: передать кэш v2 с SHA-256 на демо-машину, выполнить там проверки. Baseline не является нейросетевым подбором; скрытого fallback нет.

Техническая проверка не заменяет человеческую приёмку. Обязательность отсутствующего JSONL и условия распространения исходных данных вне хакатона остаются вопросами к организатору, а не выдуманными требованиями команды.

Финальная проверка: код `431c163`, включая новую интеграцию P1 `0b10f4e` и
документацию P3 `3dc7988`. Чистый Git-архив: 193 passed, 5 skipped, 48 subtests;
с локальным кэшем: 196 passed, 2 skipped, 63 subtests; локальная модель/CLI:
35 tests, OK. Протокол — `docs/acceptance.md`.

# Текущее состояние работы

Обновлено участником 1, 23.09.2026. Проверенная итоговая реализация: `8e5e1df`.

## Сейчас

- Задача #79-lite: локальный подбор до трёх подрядчиков с обязательными фильтрами и объяснениями.
- Включены все переданные изменения: P1 `7c0a191`, P2 `0dbee09` и `9a554ce`, P3 `7309b63`. История сохранена merge-коммитами `10a7fc9` и `3553740`.
- Одна реализация находится в корне. `ready_project/` — только совместимые команды запуска, без копий ядра/данных/тестов. Инструкции — в корневом README.
- UI включает выбор пяти реальных демо. Полные ответы P3 перенесены в `demo_queries.json` и проверяются `python run_demo.py --verify`.
- Ошибка объяснения для языка `РУССКИЙ` исправлена P2. Интеграционные тесты проверяют регистр/пробелы для города, формата, категории и языка.
- Проверки рабочей копии: **193 passed, 5 skipped, 48 subtests passed**; все 5 полных демо-ответов совпали. Пропущены только проверки настоящей модели/кэша.
- Команда подготовки semantic-кэша включена, но baseline остаётся режимом по умолчанию. Артефакты модели/кэша не переданы в эту копию.

## Контракт и данные

`load_contractors(path)` → `recommend(query, contractors)` → ranking всех допустимых → один срез [:3] → карточки. Сигнатуры и роли не изменены.
CSV по умолчанию: `docs/hackathon dataset anonymized .csv`, 66 профилей; путь относительно приложения, переопределение `CONTRACTORS_DATA`.
Pydantic отклоняет неконечные/неположительные часы; `None` сохраняет прежнее значение. Источники карточек, их количество и порядок защищены ядром.

## Владение

- P1: contracts/data_loader/recommender, общие зависимости, свои тесты, интеграция и общие документы.
- P2: ranking/scoring/semantic/explanations, свои тесты и ranking_notes.
- P3: UI, README, демо, UI/end-to-end тесты и человеческая приёмка.
- Перенос опубликованной сборки P3 и совместимые точки запуска выполнены P1 по запросу устранить противоречие двух сборок. Журналы P2/P3 не переписывались.

## Осталось

1. Человек проверяет полезность объяснений без имён, включая имена внутри цитат; автоматические тесты этого не заменяют.
2. Если выбран semantic: доставить кэш v2 и закреплённую модель/среду подготовки, проверить SHA-256 на целевой машине и повторить optional-тесты. Новый формат текста несовместим со старым кэшем v1; скрытого fallback нет.
3. Итоговая публикация производится обычным fast-forward без force; фактический remote hash проверяется после push.

Подробный протокол установки, проверки и источники: `docs/integration_notes.md`.

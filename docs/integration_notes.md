# Передача сборки участникам 2 и 3

## Итоговая сборка 23.09.2026 — код `8e5e1df`

Эта секция заменяет прежние сведения ниже. Старый протокол сохранён как история.

- Источники: P1 `7c0a191`, P2 `0dbee09` и `9a554ce`, P3 `7309b63`.
  Merge-коммиты `10a7fc9` и `3553740` сохраняют вклад участников.
- Одна реализация находится в корне. Из `ready_project/` удалены только
  дубли кода, тестов, данных и документов; они доступны в Git-истории.
  Совместимые `app.py` и `run_demo.py` запускают корневые файлы, requirements
  ссылаются на общие зависимости. Архив для передачи должен содержать весь проект.
- Перенесены UI-выбор демо, пять полных ответов P3 и end-to-end тесты.
  `demo_queries.json` хранит исходные замеры P3; ответы повторно проверены,
  а не переписаны под новую реализацию. CLI корректно выводит Unicode на Windows.
- Включено исправление регистра/пробелов. Дополнительные сквозные регрессии
  проверяют язык, формат, категорию и город в baseline.
- Включена команда подготовки semantic-кэша P2; runtime остаётся baseline.
  Реальных модели/кэша здесь нет. Для semantic нужен кэш v2, закреплённая среда
  подготовки, проверка SHA на целевой машине и повтор optional-тестов.
- Новых сторонних зависимостей не добавлено; общий lock прежний, 52 пакета.

### Выполненные проверки

Windows / Python 3.12.14, рабочая копия и чистый clone кода `8e5e1df`:

```powershell
.\.venv\Scripts\python.exe -m pip install --only-binary=:all: -r requirements.lock
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m pytest -q -ra
.\.venv\Scripts\python.exe run_demo.py --verify
```

В обеих копиях: **193 passed, 5 skipped, 48 subtests passed**;
`No broken requirements found`; `OK: 5 saved responses match the real core`.
Пропуски — три теста настоящего кэша и два теста локальной модели/подготовки.
Новый venv чистой копии: `C:\ha\tmp\release-clean-8e5e1df\.venv`, без
system-site-packages; wheels частично взяты из pip-кэша. Это не тест новой
загрузки всех файлов из интернета. Скопированы только tracked-файлы коммита.

AppTest проверяет настоящие root/ready_project UI и пять демо без подмены backend.
HTTP-сервер через `ready_project/app.py`: `/` и `/_stcore/health` — 200,
health-тело `ok`; временный сервер остановлен. Локальные ссылки README существуют.
Основной запуск: `.\.venv\Scripts\python.exe -m streamlit run app.py --server.address=127.0.0.1`.

Цель публикации — та же проверенная история в main и feature/core, обычный
fast-forward без force. Итоговый remote hash сверяется после push.
Человеческая приёмка объяснений без имён остаётся открытой; её не подменяют тесты.

## Архив: предыдущая интеграция `f8b920a`

Проверено участником 1 23.09.2026. Кодовый commit: `f8b920a`, ветка
`feature/core`. Это локальная интеграция; push и обновление `main` не выполнялись.

## Что объединено

- Ядро: `8dc60a2`; ranking: `c02f788`; UI/документы: `f317487`.
- Merge-коммиты `8169ba1` и `53c38cd` сохраняют исходные коммиты участников.
- Общие прямые зависимости: Pydantic 2.13.5, pytest 9.1.1, Streamlit 1.58.0.
- `requirements.lock` фиксирует все 52 пакета проверенной среды Windows / Python 3.12.14.
- UI по умолчанию читает `docs/hackathon dataset anonymized .csv` относительно
  `app.py`, независимо от рабочего каталога. Переопределение: `CONTRACTORS_DATA`.
- `duration/max_hours`: только положительные конечные числа либо `None`;
  `inf`, `-inf`, `NaN` и boolean отклоняются Pydantic. Значение `None` сохранено.
- Ядро проверяет сохранность всех исходных полей после ranking и сборки карточек;
  изменяемый вход сборщика больше не может обойти проверки состава/порядка.
- Устаревший тест отсутствующего ranking теперь явно моделирует отсутствие модуля.
  В четырёх JSON-выводах subprocess-тестов P2 включено ASCII-экранирование Unicode:
  это устраняет ошибку CP1251 на символе ₸ без изменения данных и алгоритма.

## Запуск и проверки

В корне копии проекта, PowerShell, установленный Python 3.12:

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --only-binary=:all: -r requirements.lock
.\.venv\Scripts\python.exe -m pip check
$env:RANKING_MODE = "baseline"
.\.venv\Scripts\python.exe -m pytest -q -ra
.\.venv\Scripts\python.exe -m streamlit run app.py --server.address=127.0.0.1 --browser.gatherUsageStats=false
```

На проверочной машине Python 3.12 запускался по полному пути, а не через `py`.
Готовая среда основной рабочей копии: `C:\ha\.venv`; активация не обязательна.
Первая установка требует доступа к PyPI. Работа baseline не требует модели,
внешнего API, ключей или личного аккаунта. Смена режима требует перезапуска процесса.

Общий прогон: **165 passed, 3 skipped, 45 subtests passed**. Пропущены только
две проверки с настоящим semantic-кэшем и одна с локальной моделью.
`pip check`: **No broken requirements found**. Lock совпал с `pip freeze`.
Отдельно запущен настоящий HTTP-сервер Streamlit на loopback: `/` — 200,
`/_stcore/health` — 200 / `ok`; проверочный процесс остановлен.

Чистая копия: `C:\ha\tmp\integration-clean-f8b920a`, создана локальным
`git clone --no-hardlinks --single-branch --branch feature/core C:\ha ...`.
Новый venv, установка только из `requirements.lock`; повторный полный прогон:
**165 passed, 3 skipped, 45 subtests passed**, `pip check` без ошибок.
Пакеты при второй установке брались в том числе из pip-кэша; это не проверка
новой загрузки всех wheels из интернета. Копия использует только tracked-файлы
commit f8b920a, не незакоммиченные файлы рабочей директории.

## Реальные сценарии, уже проверенные интеграционными тестами

Данные: официальный CSV, 66 записей. SHA-256 файла:
`6a724b6b7dfb5973343e68ba18dadb60fc807d87e3d78f03ee86fb26cb089f7d`.
`meta.dataset_hash` нормализованного каталога:
`e8e7e171b40bd17b5b97e029642f2de1c7225bb126f34bdc296b002252536dca`.
Это разные хэши: байты файла и канонизированные профили соответственно.

Использованы запросы пяти `prepared_cases` участника 3. Проверены реальные
`load_contractors → recommend → ranking → build_cards` и Streamlit AppTest
без подмены модулей, включая запуск UI из другого рабочего каталога.

| Сценарий | Status | Группа / допущено / показано | ID по порядку |
|---|---|---|---|
| dense | success | 10 / 6 / 3 | HK-88430, HK-44923, HK-29829 |
| date_b | success | 10 / 3 / 3 | HK-29829, HK-27222, HK-44733 |
| rare | success | 2 / 2 / 2 | HK-39372, HK-90001 |
| no_match | no_matching_candidates | 10 / 0 / 0 | — |
| missing_category | category_not_found | 0 / 0 / 0 | — |

Фактический режим: `initial_price_then_id`, версия `baseline-price-v1`.
Повторы, перестановка каталога и новый процесс проверены тестами.
HK-88430 и HK-44923 исчезают из выдачи 08.10: дата есть в их `busy_dates`,
а настоящий ответ содержит `primary_reason=busy`.

Для ручной проверки ядра: Алматы, 06.10.2026, корпоратив, Ведущий,
1 000 000 ₸, 6 часов, русский. Группа: HK-44733, HK-88430, HK-42352,
HK-44923, HK-35215, HK-77838, HK-72938, HK-27222, HK-75012, HK-29829.
Исключены HK-77838 (занят), HK-72938/HK-75012 (цена выше бюджета),
HK-42352 (нет нужного формата). Остальные шесть допустимы; показаны первые три
из таблицы, по начальной цене, а не по оценке качества.

## Что осталось владельцам

**Участнику 3:** обновить README и сохранить полные реальные ответы пяти
сценариев в свои демо-артефакты. Их текущий текст «ядро отсутствует / JSONL
блокирует запуск» устарел. В этом интеграционном изменении README,
`demo_queries.json`, `docs/demo.md` и `docs/acceptance.md` не переписывались.
Таблица выше — проверка интеграции, не замена сохранённым демо и ручной приёмке.

**Человеческая приёмка не выполнена:** скрыть имена не только в заголовках,
но и в цитатах описаний; проверить отличимость и полезность объяснений.
Уникальность строк в тестах не доказывает их смысловую полезность.

**Участнику 2, нормализация:** выявлен воспроизводимый дефект API даже в baseline:
запрос `dense` с `language="РУССКИЙ"` проходит фильтр, но объяснение HK-88430
ошибочно говорит, что язык не указан. UI отправляет канонические значения,
поэтому пять UI-сценариев этого дефекта не вызывают. Нужны одинаковые правила
`strip/casefold` для сравнений условий в ядре и объяснениях, с сохранением
исходных значений для показа. Production-модули участника 2 здесь не менялись.

**Если команда выбирает semantic:** режим пока не включён в итоговую сборку.
Кэш из временного каталога на машине P2 не является доступным артефактом этой копии.
P2 + P1 должны передать кэш с проверяемым SHA-256, закреплёнными моделью/revision
и encoder-зависимостями, обеспечить воспроизводимую подготовку и покрытие
всех допустимых сочетаний формат/категория из UI. `semantic.query_text` сейчас
зависит от регистра; изменение нормализации требует согласованной версии
текста, пересборки кэша и повторных тестов. Нет кэша/нужного ключа — явная
ошибка, не скрытый переход на baseline. После этого выполнить 3 optional-теста
и те же сквозные UI-сценарии в выбранном режиме.

## Source gate: Streamlit

Решение **approve with fallback**, дата проверки 23.09.2026.
Нужен существующему UI участника 3; новый UI-фреймворк не вводился.
[Официальный пакет 1.58.0](https://pypi.org/project/streamlit/1.58.0/):
Apache-2.0, Python >=3.10, стабильный релиз, канонический проект
[streamlit/streamlit](https://github.com/streamlit/streamlit).
Проверяемость через официальный
[AppTest](https://docs.streamlit.io/develop/api-reference/app-testing).
Это уже выбранная и проверенная P3 версия, не заявление о новейшем релизе.

Проверены [опубликованные advisories](https://github.com/streamlit/streamlit/security/advisories),
включая Windows SSRF
[GHSA-7p48-42j8-8846](https://github.com/streamlit/streamlit/security/advisories/GHSA-7p48-42j8-8846):
исправлен до выбранной версии. Это не исчерпывающий аудит всех транзитивных пакетов.
Все 52 пакета установились из wheels без локальной компиляции; точные версии
раскрыты в lock. Риски: объём зависимостей, сеть для первой установки,
проверенная платформа пока только Windows / Python 3.12. Локальный запуск
ограничен 127.0.0.1. Резерв для диагностики — прямой вызов `recommend`,
но он не заменяет требуемую приёмку интерфейса.

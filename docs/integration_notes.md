# Итоговая интеграция #79-lite

Дата: 23.09.2026. Выполнена по прямому поручению пользователя; целевая ветка —
`main`. Запускать приложение из корня, согласно README.

## Состав

- P1 `7c0a191`: актуальное ядро, загрузчик, конечные часы, защита состава/порядка карточек.
- P2 `9a554ce`: актуальные ranking/scoring/explanations/semantic, нормализация strip/casefold, версия текста v2, CLI подготовки кэша.
- P3 `7309b63`: UI с выбором демо, `run_demo.py`, пять полных ответов и сквозные тесты.
- История сохранена merge-коммитами `1a16265` и `285a720` поверх P1. Полные исходные SHA: `BUILD_SOURCES.json`.
- `ready_project/` содержала старые копии ядра/ranking. Новые материалы P3 перенесены в корень, дубликат реализации удалён. Совместимые точки входа P1 сохранены из `main` (`0b10f4e`). README и общие документы актуализированы.
- Production-модули ядра совпадают с P1, модули ranking — с P2. Контракт не менялся.
- Для subprocess-проверок с Unicode явно согласованы UTF-8 для stdout pipe и декодирование результата; это не меняет подбор.

## Проверки

Новый venv macOS/Python 3.12.13, установка всех пакетов из `requirements.lock`
с `--only-binary=:all:` успешна; `pip check` без конфликтов.
Baseline: **186 passed, 5 skipped, 48 subtests passed**. Пять сохранённых ответов
совпали с настоящим `recommend`; все пять прошли Streamlit AppTest.
HTTP `/` — 200, health — 200 / `ok`; проверочный сервер остановлен.
Подробный и дополняемый протокол проверки архива — `docs/acceptance.md`.

С локальным реальным кэшем v2: **189 passed, 2 skipped, 63 subtests passed**.
Эти проверки не означают доставку кэша или весов: они остаются вне Git.
Baseline является основным режимом; neural inference в нём нет.

## Данные и реальные результаты

Официальный CSV, 66 записей. SHA-256 файла:
`6a724b6b7dfb5973343e68ba18dadb60fc807d87e3d78f03ee86fb26cb089f7d`.
Нормализованный `meta.dataset_hash`:
`e8e7e171b40bd17b5b97e029642f2de1c7225bb126f34bdc296b002252536dca`.

| Сценарий | Статус | Группа / допущено / показано | ID по порядку |
|---|---|---|---|
| dense | success | 10 / 6 / 3 | HK-88430, HK-44923, HK-29829 |
| date_b | success | 10 / 3 / 3 | HK-29829, HK-27222, HK-44733 |
| rare | success | 2 / 2 / 2 | HK-39372, HK-90001 |
| no_match | no_matching_candidates | 10 / 0 / 0 | — |
| missing_category | category_not_found | 0 / 0 / 0 | — |

Режим `initial_price_then_id`, версия `baseline-price-v1`. В паре дат 06/08.10
HK-88430 и HK-44923 исключены из-за `busy_dates`; причина видна в ответе.
Полные запросы и ответы: `demo_queries.json`, `docs/demo_responses.json`.

## Осталось перед демонстрацией

- Людям проверить понятность и различимость объяснений, скрыв имена также внутри цитат. Автотесты не заменяют смысловую оценку.
- Проверить доступ жюри к репозиторию либо передать архив итогового коммита.
- Если выбран semantic: передать v2 кэш и SHA-256 на демо-машину, повторить проверки согласно `ranking_notes.md`. CLI подготовки уже включён; старый v1 несовместим.
- Итоговая повторная установка на Windows/Linux не выполнялась; исторические Windows-проверки предыдущей сборки не объявляются проверкой этой версии.

## Source gate: Streamlit

Решение **approve with fallback**, дата проверки 23.09.2026.
Нужен существующему UI участника 3; новый UI-фреймворк не вводился.
[Официальный пакет 1.58.0](https://pypi.org/project/streamlit/1.58.0/):
Apache-2.0, Python >=3.10, стабильный релиз, канонический проект
[streamlit/streamlit](https://github.com/streamlit/streamlit).
Проверяемость через официальный
[AppTest](https://docs.streamlit.io/develop/api-reference/app-testing).
Это уже выбранная и проверенная P3 версия, не заявление о новейшем релизе.

Проверены [опубликованные advisories](https://github.com/streamlit/streamlit/security/advisories),
включая Windows SSRF
[GHSA-7p48-42j8-8846](https://github.com/streamlit/streamlit/security/advisories/GHSA-7p48-42j8-8846):
исправлен до выбранной версии. Это не исчерпывающий аудит всех транзитивных пакетов.
Все 52 пакета установились из wheels без локальной компиляции; точные версии
раскрыты в lock. Риски: объём зависимостей, сеть для первой установки,
первоначальная проверка — Windows / Python 3.12; при финальной интеграции
тот же lock установлен также на macOS / Python 3.12.13. Локальный запуск
ограничен 127.0.0.1. Резерв для диагностики — прямой вызов `recommend`,
но он не заменяет требуемую приёмку интерфейса.

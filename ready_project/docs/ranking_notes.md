# Участник 2: ranking, semantic matching и объяснения

Текущее состояние: A21/A22 готовы в `f608398`; A23 реализован и проверен
локально с настоящей моделью. По умолчанию действует baseline; semantic
включается явно при запуске с закреплённым кэшем. Сигнатуры не изменились.
Объединение с ядром и реальное JSONL-демо ещё не проверены.
Следующий ограниченный шаг A24: объяснения теперь отражают дату, запрошенный
язык и длительность; результаты проверки и границы — в конце этого документа.
После публикации демо-снимка P3 `5f85877` проверены также пять его подготовленных
списков кандидатов; таблица TOP-3 и воспроизводимые регрессии приведены ниже.

## A23 — source gate перед изолированным экспериментом, 23.09.2026

Decision: **approve with fallback для временного CPU-эксперимента**; изменения
общих зависимостей и выбор режима приложения остаются у интегратора.
Кандидат: `sentence-transformers==6.1.0`, `transformers==5.17.0`, `torch==2.14.0`.
Модель: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, revision
`e8f8c211226b894fcb81acc59f3b34ba3efd5f42`. Лицензии ST/Transformers/модели —
Apache-2.0, PyTorch — BSD-3-Clause. Назначение: локальные embeddings русского
description и явно заданных категории/мероприятия; не фильтрация и не генерация фактов.

Первичные источники: [PyPI ST](https://pypi.org/project/sentence-transformers/6.1.0/),
[API](https://sbert.net/docs/package_reference/sentence_transformer/model.html),
[карточка модели](https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2),
[закреплённая ревизия](https://huggingface.co/sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2/commit/e8f8c211226b894fcb81acc59f3b34ba3efd5f42).

384 измерения; стандартное ограничение модели 128 токенов — риск потери конца
длинного описания. Веса safetensors около 471 MB, весь репозиторий модели не нужен.
Эксперимент: отдельный venv в /private/tmp, официальные wheels PyPI, CPU,
trust_remote_code=False, без токенов/аккаунта и без передачи описаний наружу.
Полный граф версий, холодная установка, inference и качество проверяются отдельно;
отсутствие опубликованных advisories не означает полного security-аудита.
Базовый режим остаётся доступным явно; автоматический fallback запрещён.
Общие requirements/lock пока не меняются.

Ветка: `feature/ranking`. Получен опубликованный контекст `82a8225` через merge
`d5ab8b7`; `codex_project_context/PROJECT_SPEC.md` сохраняет прежний контракт
`codex_team_plan/TEAM_SPEC.md`, раздел 4. Участник 2 также ведёт свой
`codex_project_context/progress/P2.md`; общую доску редактирует интегратор.

## Границы и вызов для интегратора

Принадлежащие участнику 2 файлы: `ranking.py`, `scoring.py`, `semantic.py`,
`explanations.py`, три `tests/test_*.py` и этот документ.
Загрузку, обязательные фильтры, contracts, UI, README и зависимости не менял.

```python
from ranking import rank_candidates, RANKING_METHOD, SCORING_VERSION
from explanations import build_cards

# eligible — результат обязательных фильтров участника 1
ordered = rank_candidates(eligible, query)
results = build_cards(ordered[:3], query)
# meta.ranking_method = RANKING_METHOD
# meta.scoring_version = SCORING_VERSION
```

`rank_candidates` возвращает всех переданных кандидатов, `build_cards` сохраняет
состав и порядок переданных финалистов. TOP-3 срез выполняется только ядром.
Ни одна функция не мутирует входные профили или query, включая вложенные списки.
Неподходящие профили модуль не фильтрует и из каталога ничего не добавляет.

Вход — нормализованные словари из общего контракта. ID: непустая строка или
целое число; после `str(id)` ID должны быть уникальны (`1` и `"1"` конфликтуют).
Цена — конечное неотрицательное число, description — строка, флаги — bool,
списки форматов/языков — списки. Query с положительным budget, выбранной category
и допустимыми остальными полями проверяет ядро. Отсутствующее description
обрабатывается как пустое. Ошибки ID/цены/типа description вызывают ValueError;
отсутствие обязательного ключа — KeyError. Их нельзя выдавать за отсутствие кандидатов.

Добавляемые поля ranking: `score`, `score_breakdown`, `evidence`.
Карточки добавляют `category`, `explanation`; исходные поля и флаги сохраняются.
Флаги synthetic/price_imputed/city_imputed обязан показывать UI; объяснение
дополнительно отмечает подготовленные цену и город. Synthetic не влияет на score.

## Формула и фактический режим

- `RANKING_METHOD = "initial_price_then_id"`.
- `SCORING_VERSION = "baseline-price-v1"`.
- `score = round(-float(price_from_kzt), 6)`.
- `score_breakdown = {"initial_price": score}`.
- Порядок: score по убыванию → исходная числовая цена по возрастанию → `str(id)`.

Это прозрачный порядок по начальной цене среди уже допустимых профилей, а не
оценка качества. Отрицательный score допустим, он не является вероятностью
или процентом. Одинаковые обязательные критерии не дают бонусов. Часы null,
дополнительные языки, synthetic и длина описания не улучшают позицию.
Нет случайности, времени, сетевых вызовов или скрытого переключения режима.

**В режиме baseline нейросетевое inference не используется.**
Для baseline `semantic.py` предоставляет `lexical_excerpt_v1`: точное совпадение слов
event_type/category с описанием для выбора цитаты. Оно не влияет на score
и не заменяет смысловое сравнение. Нормализация: casefold, ё → е; без стемминга,
синонимов и скрытых пожеланий. При равенстве берём более раннее предложение.

## Evidence и объяснения

Evidence — список `{source: "profile" | "query", field, value}`. Для description
также сохраняются `start`, `end` (Python-индексы символов, конец исключается)
и отсортированные `matched_terms`. Проверка:
`profile["description"][start:end] == evidence["value"]`.

Объяснение содержит две шаблонные фразы: начальная цена «от», арифметическая
разница с лимитом и заявленный формат; затем буквальная цитата с атрибуцией
«В описании указано». Разница вычисляется десятичной арифметикой. Это не
обещание итоговой сметы, качества, экономии, свободной даты или бронирования.
Причина порядка — цена, цитата даёт дополнительный контекст профиля.

Берётся полное короткое предложение до 320 символов. Точки десятичных чисел,
распространённые сокращения, инициалы и `;` сохраняются. Длинное предложение
не обрезается: отрезанная оговорка могла бы изменить смысл. Если короткого
фрагмента нет, сообщаем, что цитата не приведена и это не означает отсутствия
опыта. Выбор предложения эвристический; редкие сокращения и контекст соседних
предложений остаются предметом человеческой проверки. Точки внутри числа
или сокращения не являются дополнительными предложениями карточки.

Одинаковые объяснения не маскируем косметическими различиями: карточки
сообщают о совпадении приведённых фактов среди показанных. Уникальность,
«лучший» и «единственный самый дешёвый» не заявляются.

## Проверка главного риска и источников, 23.09.2026

Применены локальные `hackalem-case-selection` и `hackalem-source-gate`:
выбор #79-lite уже передан командой; повторный выбор продукта не выполнялся.
Точный текст задания и критерии в CHALLENGE_BRIEF, актуальное распределение
в WORK_STATE ещё не внесены — задача интегратора. Для этого этапа используем
явно переданный пользователем участок и командный TEAM_SPEC, не выдавая
пересказ плана за официальный первоисточник.

CSV из репозитория прочитан только для анализа риска; 66 строк, уникальные ID,
пустых описаний нет, длины 68–1203 символа. Из 55 профилей с форматом «свадьба»
32 не содержат основы `свад` в description. Следствие: отсутствие слова
не доказывает отсутствие опыта; лексическое совпадение не вводим в допуск.
Источник: `docs/hackathon dataset anonymized .csv` (переданный командой материал).
Source gate: **approve для локального чтения предоставленного CSV**;
новый датасет, модель, пакет или сервис не добавлены, внешняя передача отсутствует.
Отдельная лицензия/полный текст условий использования в этой проверке не установлены.
CSV не подменяет зафиксированный для приложения источник JSONL.

Независимое read-only ревью обнаружило потерю отрицания при разрезании по `;`
и риск обрезки оговорок по длине. Исправления закреплены отдельными тестами.

## Запуск и проверенные примеры

Зависимости: только стандартная библиотека Python. Проверено на Python 3.12.13.

```sh
python3 -B -m unittest discover -s tests -p 'test_*.py' -v
```

Первый этап включал 30 модульных тестов: 0/1/2/>3 кандидатов, все 24 перестановки четырёх профилей,
tie-break/округление, повторы, три отдельных процесса с разными PYTHONHASHSEED,
отсутствие мутаций, конечность score, отклонение дубликатов ID, факты и цитаты,
точная арифметика, флаги, null, отсутствие выдуманных различий, отрицания и
сохранение состава карточек. Результат после финальной проверки фиксируется
в отчёте этапа; это не проверка загрузчика, фильтров или полного приложения.

Исторический пример текста A21/A22 на **явно синтетических тестовых данных**,
не демо реального каталога (A24 дополнительно показывает календарь/условия):
query: свадьба, Ведущий, Алматы, 2026-11-14, бюджет 300 000 ₸;
у A/B/C начальная цена 220 000 ₸, descriptions соответственно
«Ведущий с гитарой.», «Ведущий с фортепиано.», «Ведущий с викторинами.».
Все три условно уже допущены ядром. Ожидаемый порядок A → B → C по строковому ID.

> Начальная цена — от 220 000 ₸, на 80 000 ₸ ниже лимита 300 000 ₸; в каталоге указан формат «свадьба». В описании указано: «Ведущий с гитарой».

> Начальная цена — от 220 000 ₸, на 80 000 ₸ ниже лимита 300 000 ₸; в каталоге указан формат «свадьба». В описании указано: «Ведущий с фортепиано».

> Начальная цена — от 220 000 ₸, на 80 000 ₸ ниже лимита 300 000 ₸; в каталоге указан формат «свадьба». В описании указано: «Ведущий с викторинами».

Человеческая проверка: без имён сопоставить эти три объяснения с descriptions.
Затем повторить на реальных финалистах из ядра; синтетический пример не заменяет
проверку содержательности объяснений реального каталога.

## Следующий этап и запрос интегратору

1. Подтвердить сигнатуры и подключить rank → срез [:3] → cards в ядре.
2. Предоставить согласованный JSONL; подтвердить актуальность TEAM_SPEC и
   внести назначение в WORK_STATE. JSONL и код ядра сейчас отсутствуют.
3. A23 реализован: интегратору принять настройки режима и артефакт кэша;
   для подготовки собственного кэша согласовать отдельное окружение модели.
   Изменений общих dependencies и .env.example в нашей ветке нет.
4. Провести реальную проверку TOP-3 и объяснений, пары дат и замер времени.
   Результаты полного приложения, реальные demo-запросы и скорость не заявлены.

## A23: реализованный semantic-режим

`score = round(cosine(description_embedding, query_embedding), 6)`.
`score_breakdown = {"semantic_similarity": score}`. Диапазон [-1, 1],
не вероятность, не оценка качества и не доказательство опыта. Нет комбинации
с произвольно подобранными весами: при равенстве score идёт цена, затем ID.
Цена проверяется на валидность даже в semantic-режиме. Фильтры не меняются.

Текст запроса: `Тип мероприятия: {event_type}. Категория подрядчика: {category}.`.
Город, дата, бюджет, язык и часы не используются для придумывания стиля/опыта;
они остаются параметрами допуска ядра. Одинаковый смысловой запрос имеет
один вектор; дата меняет состав допустимых, а не описание желания пользователя.

`encode_with_local_model` получает уже загруженную закреплённую модель.
Каждый текст разбивается на части в пределах реального max_seq_length
токенизатора, по возможности на границе слова. Склейка частей точно равна
оригиналу. Модель кодирует части на CPU с batch_size=1, float32 и нормализацией;
нормированные векторы усредняются с весом числа содержательных токенов части
(для этой BERT-модели вычитаются 2 специальных токена), результат нормируется.
Это эвристика обработки длинного текста, не обученные веса ранжирования.
`eval`, один CPU-поток и deterministic algorithms задаются при подготовке.

`prepare_semantic_cache(candidates, queries, encode, *, model, revision, encoder)`
возвращает канонические UTF-8 JSON bytes. Тексты сортируются и дедуплицируются;
ключ — SHA-256 точного текста. Кроме описаний и запросов кодируются полные
короткие предложения. Версия подготовки: `event-category-ru-excerpts320-v1`.
Публичные функции ranking/cards по-прежнему имеют ровно два аргумента.

В semantic-режиме цитата — предложение с максимальной смысловой близостью
к тому же запросу, при равенстве — более раннее в исходнике. Это дополнительный
контекст; score кандидата вычислен по всему description, а не одной цитате.
В evidence сохраняются роль `semantic_scoring`, полное исходное описание,
query_text, similarity, model, revision и cache_sha256; для `semantic_excerpt`
также буквальный value/start/end и similarity предложения. Карточки проверяют
соответствие доказательств профилю, запросу и score. Цитата может содержать
саморекламу или ошибочное утверждение автора; она явно атрибутирована профилю.

### Как включить без новых runtime-зависимостей

При обычном запуске ничего не скачивается и не устанавливается. Нужны только
Python и заранее подготовленный JSON-кэш. Все настройки считываются один раз
при импорте ranking; изменения файла или environment не меняют живой процесс.

```sh
RANKING_MODE=semantic \
RANKING_SEMANTIC_CACHE=/absolute/path/embeddings.json \
RANKING_SEMANTIC_SHA256=<полный_SHA256_этого_файла> \
python3 <точка_входа_интегратора>
```

Пример выше требует подстановки пути/контрольной суммы; команда приложения ещё
не определена владельцем ядра. Файл читается в неизменяемый снимок один раз.
Метаданные импортировать из `ranking`, не из baseline-констант `scoring`:
`RANKING_METHOD="cached_sentence_embeddings_cosine"`,
`SCORING_VERSION="semantic-cosine-v1:<полный хэш кэша>"`.

Неверный mode, отсутствующий/повреждённый файл, несовпадение SHA, нулевой/NaN
вектор, незакреплённая revision, неизвестный текст описания/запроса — явная
`SemanticCacheError`. Автоматического перехода на цену нет. Резервный режим
выбирается оператором до нового запуска: `RANKING_MODE=baseline`.
Изменение данных требует новой подготовки и явной смены хэша; прежний кэш
можно оставить для воспроизведения старого результата.

### Подготовка артефакта интегратором

Только отдельное окружение подготовки требует пакетов из source gate.
Не добавлять их в runtime requirements без решения участника 1. Скачать из
официального репозитория закреплённую revision, только safetensors, tokenizer
и конфиги (без ONNX/TF/OpenVINO копий). Проверенная SHA-256 model.safetensors:
`eaa086f0ffee582aeb45b36e34cdd1fe2d6de2bef61f8a559a1bbc9bd955917b`.

После загрузки snapshot, в offline-окружении:

```python
import hashlib
import itertools
from pathlib import Path
import torch
from sentence_transformers import SentenceTransformer
from semantic import prepare_semantic_cache, encode_with_local_model

torch.set_num_threads(1)
torch.use_deterministic_algorithms(True)
model = SentenceTransformer(local_snapshot_path, device="cpu", local_files_only=True,
                            trust_remote_code=False, model_kwargs={"use_safetensors": True})
model.eval()
# contractors получает согласованный загрузчик участника 1 из утверждённого JSONL.
queries = [{"event_type": event, "category": category} for event, category in itertools.product(
    sorted({v for c in contractors for v in c["event_formats"]}),
    sorted({v for c in contractors for v in c["categories"]}))]
payload = prepare_semantic_cache(
    contractors, queries, lambda texts: encode_with_local_model(model, texts),
    model="sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2",
    revision="e8f8c211226b894fcb81acc59f3b34ba3efd5f42",
    encoder="ST6.1.0-T5.17.0-torch2.14.0-cpu-token-chunks-weighted-v1",
)
Path(output_cache_path).write_bytes(payload)
print(hashlib.sha256(payload).hexdigest())
```

В source gate проверены лицензии и официальные источники, а совместимость
подтверждена на Python 3.12.13/macOS 15.7.9 arm64. Разрешённый эксперимент
не является полным аудитом всех транзитивных зависимостей. Установлен 41 пакет;
точные версии временного окружения сохранены локально в
`/private/tmp/hackalem-semantic.p3n3Jp/versions.txt`. Пины основного стека:
ST 6.1.0, transformers 5.17.0, torch 2.14.0, numpy 2.5.3, tokenizers 0.23.2,
safetensors 0.8.0, huggingface-hub 1.32.0. Интегратору: полный lock подготовки
и размещение артефакта нужно принять отдельно; временная папка не входит в Git.

<details>
<summary>Фактический freeze временного окружения подготовки (не общий lock проекта)</summary>

```text
annotated-doc==0.0.5
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
cloudpickle==3.1.2
filelock==4.0.1
fsspec==2026.9.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.32.0
idna==3.20
Jinja2==3.1.6
joblib==1.6.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.26.0
networkx==3.7
numpy==2.5.3
packaging==26.3
Pygments==2.21.0
PyYAML==6.0.3
regex==2026.9.10
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1
sentence-transformers==6.1.0
setuptools==84.0.0
shellingham==1.5.4
sympy==1.14.0
threadpoolctl==3.7.0
tokenizers==0.23.2
torch==2.14.0
tqdm==4.70.1
transformers==5.17.0
typer==0.27.2
typing_extensions==4.16.0
```

</details>

### Проверенный эксперимент с настоящей моделью

Данные: предоставленный CSV, 66 профилей и 102 пары категория/мероприятие;
дополнительно два явно тестовых описания для проверки перефразирования.
Это независимый эксперимент, **не замена источника JSONL в приложении**.
Описание про съёмку церемоний бракосочетания получило 0.549202 для запроса
«Фотограф, свадьба», водопровод — 0.083068. Это один smoke-пример, не benchmark.
25 профилей потребовали нескольких частей, максимум 3; конец текста сохранён.

Пробный отбор отдельным reference-скриптом: Алматы, 2026-09-30, свадьба,
Ведущий, бюджет 1 000 000 ₸, язык/длительность не заданы. В CSV проходят 5.
Порядок TOP-3: **HK-42352 (0.553117), HK-35215 (0.457878), HK-77838 (0.413027)**.
Отличительные цитаты: у первого — «Опыт ведения свадеб 13 лет…» (в карточке
полное предложение), у второго — перечень форматов, у третьего — описание
подачи ведущего. Все цитаты буквальные; цены 900 000 / 900 000 / 1 000 000 ₸.
У первого цена помечена price_imputed; маркер сохранён и виден в объяснении.

На одном финальном запуске: новый процесс с импортом/загрузкой модели — 3.987 s
(пакеты/веса уже на диске), подготовка кэша — 5.480 s, ranking+cards пяти
кандидатов на загруженном кэше — 0.0021 s. Это разовые замеры ноутбука,
не холодная установка с сетью, не время полного приложения и не обещание SLA.

Кэш 3 601 128 байт, SHA-256
`bd5b481f3538126ba2daf5de5c255185cdbcf8bd39c9ee0edb91c8ac8c6da09c`.
Два отдельных запуска модели дали побайтно одинаковый кэш и одинаковые карточки;
перестановка кандидатов также сохранила результат. Между разными библиотеками
или платформами идентичность нового inference не обещается; готовый кэш
фиксирует фактические векторы для демонстрации.
Отдельно проверен runtime системным Python без установленного torch: два новых
процесса с разными PYTHONHASHSEED и обратным порядком входных профилей дали
побайтно одинаковые JSON-карточки на настоящем кэше.

Локальные артефакты эксперимента (вне Git):
`/private/tmp/hackalem-semantic.p3n3Jp/{probe.py,report-first.json,report.json,embeddings.json}`.

### Тесты A23

Обычная команда без модели: 53 проверки и один явно пропущенный optional smoke.

```sh
RANKING_MODE=baseline python3 -B -m unittest discover -s tests -p 'test_*.py' -q
```

Проверка с установленным только во временный venv стеком и локальной моделью:
все 54 теста прошли, без пропусков (4.763 s в этом запуске).

```sh
RANKING_MODE=baseline \
SEMANTIC_TEST_MODEL_DIR=/private/tmp/hackalem-semantic.p3n3Jp/model \
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 HF_HUB_DISABLE_TELEMETRY=1 \
/private/tmp/hackalem-semantic.p3n3Jp/venv/bin/python -B -m unittest discover -s tests -p 'test_*.py' -q
```

Дополнительные проверки: cosine/полнота кэша/хэш/неизменяемость, NaN и нулевые
векторы, 24 перестановки в отдельных процессах semantic-режима, фиксация mode,
явный отказ при сбое, смысловой выбор буквальной цитаты, сохранение хвоста
описания, отклонение устаревшего evidence. Реальный smoke проверяет версии,
веса, перефразирование и повторное получение того же кэша без скачивания.

## A24: объяснения учитывают условия конкретного запроса

Выбрано после успешного fetch 23.09.2026: main остаётся на `82a8225`, общая
доска стартовая. В `origin/feature/ui-readme` опубликован `32281f0`; участник 3
проверил отображение карточек, но явно оставил настоящую приёмку объяснений
до появления ядра/JSONL. A21–A23 уже реализованы в нашей ветке (`8f4c2bb`),
поэтому они не выполнялись заново. A24 допускает более ранний реальный пример.

В предоставленном CSV у HK-42352 заявлено 10 ч и русский/казахский языки;
на 2026-09-30 и 2026-10-07 нет отметки о занятости. До исправления оба запроса
с длительностью 6 и 10 ч давали одинаковое explanation: дата, часы и язык
игнорировались. Это проверяемый дефект информативности нашего модуля,
не результат пользовательской приёмки всего приложения.

Теперь первая фраза дополнительно сообщает только имеющиеся в данных факты:

- Для запрошенной даты и списка busy_dates: «по календарю нет отметки
  о занятости на 2026-09-30»; это не подтверждение бронирования.
- Только при запрошенном языке: «запрошенный язык «казахский» указан в профиле».
  Дополнительные языки без запроса не выдаются за преимущество.
- Только при запрошенной длительности: «заявленные 10 ч покрывают запрошенные
  6 ч»; равенство допустимо, дробные значения сохраняются.
- max_hours=null: «услуга не привязана к присутствию на площадке», без обещания
  неограниченных часов. Отсутствующий max_hours не приравнивается к null.

Календарь отсутствует/None — утверждения о свободе нет. Если ядро передаст
противоречащие запросу данные, объяснение честно укажет занятую дату, отсутствие
указания языка или недостаточные часы. Модуль не исключает/не переставляет
профиль и не берёт на себя обязательные фильтры: такой ввод должен исправить
владелец ядра. Query по-прежнему должен быть валидирован ядром, включая окно дат.

В evidence добавлено поле-источник `profile.busy_dates` с независимой копией
значения; query.date, query.language/query.duration и соответствующие поля
профиля уже сохраняются. Формат evidence, сигнатуры, scoring, порядок, состав,
кэш embeddings и выбранные буквальные цитаты не изменены. Две текстовые фразы
сохранены; условия добавляются через `;`, а не новыми предложениями.

Два профиля с одинаковыми ценой и цитатой, но max_hours=8 и 10, теперь различимы
при запросе 6 ч по реальному значению часов. Если дополнительных условий нет
или факты одинаковы, различия не выдумываются и прежняя отметка совпадения
фактов остаётся. Это не дополнительный бонус за длительность в ranking.

Проверки A24: 9 новых тестов, включая регрессию на существующем CSV HK-42352.
Обычный запуск: 62 passed + 1 явно пропущенный optional smoke; запуск в ранее
подготовленном venv с настоящей локальной моделью: **63 passed, 5.443 s**.
Независимое read-only ревью замечаний не выявило. Полный JSONL → recommend → UI
по-прежнему не проверен; A33 и итоговая проверка A24 требуют интеграции.

Одна человеческая проверка: сравнить без имён карточки с одинаковыми ценой и
цитатой, но разными заявленными 8/10 ч, при запросе 6 ч; подтвердить, что понятно
фактическое различие и нигде не обещана гарантированная бронь/итоговая цена.

## A24: проверка подготовленных сценариев участника 3

Источник запросов и уже отобранных ID: `demo_queries.json`, commit
[`5f85877`](https://github.com/BAITC-Hacks/hack-6b3754fc-oyan-ai/blob/5f8587735736b1bff520e2e821459783aa133c70/demo_queries.json)
ветки участника 3. Снимок запросов/ID в `PreparedCatalogDemoTests` отдельно
сверен с этим файлом: совпадает. Чужой файл не копировался в рабочую ветку
и не изменялся; тесты используют свои явно обозначенные reference-фикстуры.

Профили прочитаны из предоставленного CSV, SHA-256:
`6a724b6b7dfb5973343e68ba18dadb60fc807d87e3d78f03ee86fb26cb089f7d`.
Тест не выполняет обязательные фильтры заново: передаёт нашей функции именно
списки допустимых, подготовленные участником 3. Это проверка границы P2,
а не проверенный ответ `recommend`, загрузчик JSONL или UI.

| Сценарий P3 | Передано допустимых | TOP-3 baseline | TOP-3 semantic |
|---|---:|---|---|
| dense, 2026-10-06 | 6 | HK-88430, HK-44923, HK-29829 | HK-88430, HK-44733, HK-44923 |
| date_b, 2026-10-08 | 3 | HK-29829, HK-27222, HK-44733 | HK-44733, HK-27222, HK-29829 |
| rare, 2026-10-09 | 2 | HK-39372, HK-90001 | HK-90001, HK-39372 |
| no_match | 0 | пусто | пусто |
| missing_category | 0 | пусто | пусто |

Между двумя датами в обоих режимах из показанной тройки исчезают HK-88430 и
HK-44923. В исходных busy_dates у обоих есть 2026-10-08 и нет 2026-10-06.
Все показанные карточки имеют дату своего запроса в объяснении, а в календаре
нет занятости на неё. Причины исключений и различимые бизнес-статусы двух
пустых сценариев должен подтвердить интегратор в настоящем `recommend`.

Проверены состав всего ranked-списка, максимум 3 на границе вызова cards,
буквальность и координаты цитат, значения всех evidence, сохранение synthetic
и imputed-флагов, null-hours у флористов и отсутствие мутаций входов.
После удаления anon_name строки объяснений различаются в каждом непустом
сценарии. Это автоматическая проверка различимости строк, не замена человеческой
оценки содержательности. Для обоих режимов обратный порядок входа и новый
процесс с другим PYTHONHASHSEED сохраняют весь JSON карточек.

Добавлено 5 тестов в собственный `tests/test_ranking.py`. Без локальных
артефактов ожидаются 66 passed и 2 явно обозначенных optional skips.
С ранее подготовленными моделью/кэшем прошли **все 68 тестов**:

```sh
RANKING_MODE=baseline \
SEMANTIC_DEMO_CACHE=/private/tmp/hackalem-semantic.p3n3Jp/embeddings.json \
SEMANTIC_TEST_MODEL_DIR=/private/tmp/hackalem-semantic.p3n3Jp/model \
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 HF_HUB_DISABLE_TELEMETRY=1 \
/private/tmp/hackalem-semantic.p3n3Jp/venv/bin/python -B -m unittest discover -s tests -p 'test_*.py' -q
```

Для проверки только готового semantic-кэша достаточно SEMANTIC_DEMO_CACHE и
обычного Python без ML-пакетов. По умолчанию тест закреплён на SHA-256 кэша
`bd5b481f…c6da09c` (полный hash в A23 и тесте). Для явно пересобранного артефакта
передать его полный хэш через SEMANTIC_DEMO_CACHE_SHA256; проверка контрольной
суммы не отключается. Режим, модель и подготовка должны соответствовать A23;
ожидаемый порядок не переписывать без анализа причины расхождения.

На момент этого шага ядра в remote ещё не было. Последующая проверка ниже
использует уже опубликованное ядро; его файлы по-прежнему не изменялись.

## C02/A24: стык с опубликованным ядром, 23.09.2026

Проверенный снимок P1: `origin/feature/core` =
`8dc60a2d82212bc5f8db1ef59841eaa85be00ff4`; код ядра опубликован в
`0d90a22`, дополнительные проверки в `2160eff`. Его `docs/WORK_STATE.md`
явно разрешает текущий официальный CSV через единый CSV/JSONL-загрузчик.
Main остаётся `82a8225`; эти ветки пока не объединены.

Первый настоящий вызов `recommend(query, load_contractors(csv))` выявил
`IntegrationNotReadyError: ranking.py must expose rank_candidates and build_cards`.
Ядро разрешает обе функции из `ranking`, а P2 экспортировал карточки только
из `explanations`. Добавлен публичный реэкспорт той же `build_cards` в `ranking.py`.
Алгоритм, сигнатуры, score, evidence и принадлежность среза TOP-3 не менялись.
Регрессия публичного вызова также падала до правки и прошла после неё.

В `tests/test_ranking.py` добавлены два optional теста настоящего ядра:
они автоматически включаются после объединения с ядром или при передаче
`P2_CORE_DIR` с его опубликованными модулями. Нужны зависимости P1.
Применяется двухаргументный production-вызов без подставных ranker/card_builder.
Проверены пять прежних запросов в обоих режимах, совпадение всех карточек
с reference-результатом, статусы/счётчики, фактические метаданные, отсутствие
мутаций, повтор в процессе и новый процесс с обратным порядком каталога.
У `rejections` ядро сохраняет исходный порядок: сравнивается состав по ID;
карточки, остальные поля ответа и dataset_hash совпадают целиком.

| Сценарий | Наблюдаемый status | Группа / допустимых / показано |
|---|---|---|
| dense | success | 10 / 6 / 3 |
| date_b | success | 10 / 3 / 3 |
| rare | success | 2 / 2 / 2 |
| no_match | no_matching_candidates | 10 / 0 / 0 |
| missing_category | category_not_found | 0 / 0 / 0 |

TOP-3 совпали с таблицей предыдущего раздела. Для HK-88430/HK-44923 на
08.10 ядро действительно вернуло `primary_reason=busy` и `busy` в reasons.
Semantic использовал ранее проверенный неизменённый кэш с SHA-256
`bd5b481f3538126ba2daf5de5c255185cdbcf8bd39c9ee0edb91c8ac8c6da09c`.
Новый нейросетевой inference для этой проверки не требовался.

Временный снимок ядра и объединённые тесты:
`/private/tmp/hackalem-core-check.ah3nsema`. В его venv Python 3.12.13
установлен без изменений опубликованный P1 `requirements.lock`, включая
pydantic 2.13.5 / pytest 9.1.1. Общие зависимости в нашей ветке не менялись.
Из корня feature/ranking выполнено:

```sh
P2_CORE_DIR=/private/tmp/hackalem-core-check.ah3nsema \
SEMANTIC_DEMO_CACHE=/private/tmp/hackalem-semantic.p3n3Jp/embeddings.json \
RANKING_MODE=baseline \
/private/tmp/hackalem-core-check.ah3nsema/venv/bin/python -B -m unittest discover -s tests -p 'test_*.py' -q
```

Результат: **70 passed, 1 skipped** (optional smoke самой модели), 2.282 s.
Без ядра/артефактов, обычным Python: **67 passed, 4 skipped**, 1.005 s.
Во временном объединённом снимке с кэшем `python -B -m pytest -q`:
**95 passed, 1 failed, 1 skipped**, 55 subtests passed, 2.00 s.

Адресная передача P1: падает `tests/test_core.py:193`
`test_success_requires_participant_two_module` — тест без изоляции ожидает
отсутствия `ranking`, но теперь production-вызов успешно возвращает карточку.
Для проверки отсутствующего модуля нужно явно имитировать `ModuleNotFoundError`
с `name='ranking'` на границе importlib; позитивный путь уже покрыт нашими
тестами. Чужой тест не изменён и ошибка не скрыта исключением из общего запуска.

Граница результата: подтверждён CSV → loader → recommend → ranking → cards
в отдельном снимке. UI, официальный JSONL, объединение веток и финальная
человеческая оценка объяснений остаются задачами интеграции/приёмки.
